# Gives us separation from real DB passwords. 
# Production deployment will copy env.properties file over
# this one as passwords should not be in version control. 
env.jdbc.user=portal
env.jdbc.password=portal
